КОНТЕКСТ ПРОЕКТА: калькуляторы деревянных ящиков по ГОСТ 10198-91.

ДВА РЕПОЗИТОРИЯ (не путать):
1. pakhiton79-bit/GOST_10198-91 — фронтенд-only, весь расчёт в браузере,
   единый статический HTML на калькулятор. Ветка claude/production-calculator-drawings-ngrkbs
   (НЕ main). Рабочая директория /home/user/GOST_10198-91.
2. pakhiton79-bit/GOST_backend (repo name gost_backend) — тот же продукт,
   но расчёт на сервере (Node.js/Express) + фронтенд отдаётся тем же
   сервером. Ветка main, автодеплой на Render при пуше. Не клонирован по
   умолчанию — добавляется через add_repo(owner="pakhiton79-bit", repo="GOST_backend").
   СТАТУС (обновлено 2026-09-03): бэкенд ДОГНАН до фронтенда по всем трём
   типам (I-1, I-3, II-1) - дизайн-система, override-система, negative
   guard, все диаграммы (включая Крышку/Щит торцевой/Дно/общий вид у
   II-1), бейджи каталога. Сверено 33500+ комбинациями входных данных
   (frontend vm vs backend compute, 0 расхождений) + побайтовым сравнением
   текста печатной версии для представительных кейсов всех типов/
   вариантов. Единственный пробел, как и на фронтенде: чертёж "Щит
   боковой" у II-1 ещё не готов (фото не прислано) - diagramPlaceholder().
   Если просят что-то "везде" - бэкенд теперь синхронизирован, но всё
   равно уточняй при новых правках фронтенда, нужно ли переносить их
   сюда же (независимые кодовые базы, авто-переноса нет).

ТРИ ТИПА ЯЩИКОВ, каждый ПОЛНОСТЬЮ независимый (без общих импортов между
типами — так спроектировано намеренно, баги/фиксы переносятся вручную
между файлами):
- I-1: src/i1/calc.js + src/i1/logic.js + src/i1/ui.js + src/i1/shell.html
  + src/i1/diagrams.js. Один общий параметр толщины wall.value.
  Статус: доработан, плашка на лаунчере зелёная "Исправлено".
- I-3 (два варианта — крепление за полозья / к доскам дна, из одного
  src/app.js + src/logic.js, различаются только src/variants/floor_board_*.js
  через плейсхолдер /*__FLOOR_BOARD_CALC__*/): несколько независимых
  толщин (стенки/крышка, доска дна, поперечный брус крышки, подполозная
  доска, полоз, торцовый брус дна). Плашка жёлтая "Требует проверки".
- II-1 (каркасно-щитовой, стойки+брусья+раскосины вместо сплошного
  набора досок): src/ii1/calc.js + logic.js + ui.js + shell.html +
  diagrams.js (фронтенд); backend/src/ii1/{compute.js,logic.js} +
  frontend/public/ii1.html + frontend/public/js/ii1/{calc-ii1.js,ui.js,
  diagrams/{dno,kryshka,torec}.js} (бэкенд, порт завершён 2026-09-03).
  Плашка красная "Ещё не готово" на обоих (ни разу не было полной сверки
  с реальными чертежами, хотя чертежи Дно/Крышка/Щит торцевой/общий вид
  уже готовы - остался только Щит боковой). Источник методики — файл
  заказчика «ГОСТ 10198-91 тип 2-1.docx» (если понадобится текст
  методики снова — попроси пользователя переслать docx, в репозитории
  его нет).

СИСТЕМА РУЧНОГО OVERRIDE ТОЛЩИНЫ (уже реализована во ВСЕХ типах на
фронтенде, I-1+I-3 на бэкенде): пользователь может вручную вписать
толщину прямо в ячейку таблицы деталей (contenteditable, data-role="t").
Канонические ячейки (одна на общий параметр, чтобы не было конфликтов
при дублировании значения в нескольких строках) помечены
data-override="ключ". При правке JS взводит data-user-edited="true" на
эту ячейку, прячет галочку "Расчёт выполнен" (#calcCheck) и показывает
"⚠ Таблица изменена" (#calcOutdated) — обе через display:none/inline-flex
(НЕ visibility — так было ФАНТОМНОЕ смещение кнопки "Печать", уже
исправлено). При повторном нажатии "Рассчитать" readManualOverrides()
читает ТОЛЬКО помеченные data-user-edited ячейки (иначе всё
"замораживалось" бы навечно), передаёт как input.manualOverrides в
compute-функцию. Внутри — общий паттерн ov(key, gostValue, label):
если переданное значение есть и валидно — использует его (каскадно, т.е.
подставляет в переменную, которая дальше участвует во ВСЕХ формулах, где
использовалась), иначе — расчётное по ГОСТ; если введено МЕНЬШЕ
расчётного минимума — не блокирует, но копит в belowGost и в конце
добавляет предупреждение. Правка ОДНОРАЗОВАЯ: следующий "Рассчитать" без
новой правки в этой ячейке возвращает к чистому ГОСТ. При ЛЮБОМ
применённом override добавляется ещё одно предупреждение "чертежи ниже
могут не точно отражать эти изменения" (счётчик overridesApplied внутри
ov()) — ТОЛЬКО на экране, в печать warnings вообще не попадают.
ИСКЛЮЧЕНИЕ (важно для II-1 и I-3): у полоза и торцового бруса дна
override ИЗОЛИРОВАННЫЙ, не каскадный — оба сечения выбираются из
ГОСТ-таблицы парой толщина+ширина сразу, override только толщины без
ширины нарушил бы табличную связку. Правка меняет ТОЛЬКО отображаемое
число в своей ячейке — ни наружные размеры, ни расход пиломатериала её
НЕ учитывают (реализовано отдельной t9Display/t11Display переменной,
не трогающей исходную t9/t11, которая используется в остальных
формулах). Полное описание и обоснование — коммиты
2121efc/875afad/1e60a67 (git log в src-репо).

ЗАЩИТА ОТ ОТРИЦАТЕЛЬНЫХ ЗНАЧЕНИЙ (все типы, коммит c52d7b6): в конце
каждой compute-функции — findNegativeField(result, '') рекурсивно
обходит ВЕСЬ возвращаемый объект (и таблицу деталей, и параметры
чертежей — они в одном объекте) и ищет первое отрицательное число. Если
найдено — вместо результата {error: "Расчёт дал отрицательное значение
(<путь>) — результат недостоверен..."}. '⚠' и NaN не считаются
отрицательными (осознанные отдельные случаи).

СИСТЕМА ЧЕРТЕЖЕЙ ("diagram editor" = именно этот механизм, отдельного
инструмента с таким названием в репо нет): src/common-diagrams.js
содержит renderDiagram(imgB64, altText, IW, IH, records, widthPx,
strokeScale) — общий для всех типов рендер: фото (реальная фотография
детали/узла, base64) на заднем плане + SVG-стрелки/линии + HTML-подписи
поверх, ВСЁ в НАТУРАЛЬНЫХ ПИКСЕЛЬНЫХ координатах самого фото (IW×IH —
натуральная ширина/высота фото в пикселях). records — массив объектов:
  {type:'line'|'double'|'single', x1,y1,x2,y2}  — линия/стрелка с одним
    или двумя наконечниками (headTriangle()) между двумя точками фото;
  {lx,ly,text,vertical}  — текстовая подпись, позиционируется в % от
    IW/IH (не в пикселях — labels это HTML div с left/top в %).
Один объект records может содержать оба набора полей сразу (линия С
подписью на её середине). strokeScale — толщина линий, считается через
photoStrokeScale(imgNaturalWidth) относительно эталона (Дно, 2008px
ширина → scale=10), чтобы видимая толщина не зависела от разрешения
конкретного фото. DIAGRAM_DEFAULT_WIDTH=260px/DIAGRAM_MAX_HEIGHT=240px —
итоговый размер слота на экране (высокие/портретные фото ужимаются по
высоте). diagramPlaceholder(label) — временная заглушка "Чертёж «X» ещё
не готов", именно она сейчас используется во ВСЕХ 4 узлах II-1
(src/ii1/calc.js, вызовы diagramPlaceholder('Дно'/'Крышка'/'Щит
торцевой'/'Щит боковой') внутри calculate()).
КАК ПОДБИРАЮТСЯ КООРДИНАТЫ (для новых чертежей): открой фото (Read
умеет показывать изображения), на глаз/по пропорциям определи пиксельные
координаты нужных точек (углы деталей, куда ставить стрелки размеров),
запиши records, собери (python3 build.py), проверь ВИЗУАЛЬНО через
Playwright (открыть страницу, скриншот, сравнить с фото) — итеративно
подправляя координаты, пока стрелки/подписи не лягут точно на нужные
места. Так были сделаны все существующие чертежи I-1/I-3 (src/diagrams.js,
src/i1/diagrams.js) — бери их как образец паттерна (например
diagramEndPanel1Raskosina/diagramEndPanelNoRaskosina в src/diagrams.js).

АРХИТЕКТУРА БЭКЕНДА (важно понять перед любыми правками backend/*):
чистый расчёт живёт на сервере (backend/src/<тип>/compute.js, возвращает
JSON через POST /api/<тип>/calculate — см. server.js), рендер таблиц и
чертежей — на клиенте (frontend/public/js/<тип>/calc-<тип>.js делает
fetch() и строит tablesHtml/diagram-slot'ы из JSON, т.е. та же роль, что
calculate() во фронтенд-only репозитории, только асинхронная).
frontend/public/*.html — обычные статические файлы, картинки отдаются
Express'ом как есть по /images/*.jpg (НЕ base64 через __IMG:, в отличие
от фронтенд-only репозитория — это единственное намеренное архитектурное
отличие при переносе, не баг). Таблицы Табл.4/14/19 и формулы, которые у
I-3/II-1 во фронтенде продублированы байт-в-байт ("без изменений из уже
проверенной реализации I-3") — в бэкенде НЕ дублируются, II-1's
compute.js требует их напрямую из backend/src/i3/{sections,data/table4,
data/table14,data/table19}.js. При переносе НОВОГО фронтенд-фикса в
таких общих формулах — проверь, не тронуть ли i3-файл лишний раз для I-3
заодно (или наоборот, не сломать ли I-3, правя "под II-1").

СЛЕДУЮЩИЙ ШАГ (единственный оставшийся пробел, симметрично на обоих
репозиториях): чертёж "Щит боковой" у II-1 — фото ещё не прислано
пользователем. Как только пришлют (с разметкой координат, как для
Дно/Крышка/Щит торцевой) — сначала сделать на фронтенде (см. паттерн
independent-per-photo records в src/ii1/diagrams.js, комментарий вверху
файла), проверить, затем перенести тем же способом, что и остальные три
узла II-1 (см. коммит "Добавить тип II-1 в бэкенд" в этом репозитории —
образец структуры: compute.js/logic.js на сервере, diagrams/*.js на
клиенте, копия фото в frontend/public/images/).

ВЕРИФИКАЦИЯ ПОСЛЕ ЛЮБОГО ПОРТА В БЭКЕНД (обязательно, не полагаться
только на визуальную проверку): построить vm-харнесс, который грузит
frontend-код (src/**/*.js) в изолированный vm-контекст (осторожно с
top-level `let availableThicknesses`/`roundUpToAvailable`, объявленными
внутри самого файла — они НЕ синхронизируются с ctx.<var> присвоением
между вызовами, это известная особенность Node vm; либо вырезать этот
блок из кода перед runInContext и подставлять свою версию через ctx,
либо пересобирать контекст на каждый кейс), затем прогнать сотни-тысячи
комбинаций входных данных (включая manualOverrides) через оба
compute-пути и сравнить JSON (после округления чисел, чтобы не ловить
fp-шум) — 0 расхождений это цель. Плюс Playwright: сравнить нормализованный
innerText #printArea между фронтендом (docs/*.html) и бэкендом
(frontend/public/*.html) на нескольких представительных кейсах на тип.

СТАНДАРТ РАБОТЫ (жёсткое правило от пользователя, соблюдалось весь этот
чат): НИКОГДА не "исправляй" данные/формулы по своей догадке молча —
если есть больше одной трактовки, СПРАШИВАЙ явно (AskUserQuestion), а не
делай по аналогии. После каждого изменения: rebuild → Node vm-регрессия
(сотни комбинаций L/W/H/M, проверка на исключения/NaN/отрицательные) →
Playwright в браузере (реальный клик/ввод, не только чтение кода) →
git diff --stat (убедиться, что изменились только ожидаемые файлы) →
подробный русский коммит-месседж → push. Build.py — единственный способ
собрать docs/*.html из src/*, руками docs/ не редактировать никогда.
